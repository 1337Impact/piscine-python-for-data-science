def count_in_list(arr: list[str], str: str) -> str:
    """this function counts the number of times a string appears in a list

    Args:
        arr (list[str]): list of strings
        str (str): string to count

    Returns:
        str: number of times the string appears in the list
    """
    return arr.count(str)
